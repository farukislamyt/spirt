# X/Twitter OSINT Provider

The X/Twitter provider handles supported public profile URLs and uses public-web metadata exposed by the page.

## Scope

Normalized profile fields are accompanied by evidence provenance when values are extracted from public metadata.

## Limitations

Platform responses and public metadata can change without notice. SPIRT does not bypass authentication, private content, anti-bot controls, rate limits, or access controls.
