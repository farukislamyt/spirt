# GitHub OSINT Provider

The GitHub provider handles supported public profile URLs through public-web metadata.

## Scope

Public profile metadata is normalized into SPIRT's common social-profile model and can be used as an explicit signal during cross-platform correlation.

## Limitations

Only information exposed publicly is in scope. SPIRT does not access private repositories, private profile data, credentials, or restricted resources, and does not bypass platform protections.
