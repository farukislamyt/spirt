# LinkedIn OSINT Provider

The LinkedIn provider handles supported LinkedIn profile URLs through public-web metadata.

## Scope

The provider can normalize publicly exposed profile metadata and attach evidence describing where each value came from.

## Limitations

LinkedIn page availability and metadata vary over time. SPIRT does not bypass authentication, access controls, anti-bot mechanisms, or other platform protections.
